# COMMITTER opt-in for this repository (ADR-004 of skill-COMMITTER).
# The presence of this file IS the opt-in; the keys below only RESTRICT (SPEC.md §2).
# Discovery: ~/x/GIT/run.sh (cron) finds whoever carries this marker.
enabled: true
push: true
branch_only: master
fallback: sonnet
